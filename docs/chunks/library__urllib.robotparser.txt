SECTION: library
TITLE: urllib.robotparser—  Parser for robots.txt
SOURCE: library\urllib.robotparser.html
--------------------------------------------------

# urllib.robotparser —  Parser for robots.txt

urllib.robotparser
Source code: Lib/urllib/robotparser.py
This module provides a single class, RobotFileParser , which answers
questions about whether or not a particular user agent can fetch a URL on the
website that published the robots.txt file.  For more details on the
structure of robots.txt files, see RFC 9309 .
RobotFileParser
robots.txt
robots.txt
This class provides methods to read, parse and answer questions about the robots.txt file at url .
robots.txt
Sets the URL referring to a robots.txt file.
robots.txt
Reads the robots.txt URL and feeds it to the parser.
robots.txt
Parses the lines argument.
Returns True if the useragent is allowed to fetch the url according to the rules contained in the parsed robots.txt file.
robots.txt
Returns the time the robots.txt file was last fetched.  This is
useful for long-running web spiders that need to check for new robots.txt files periodically.
robots.txt
robots.txt
Sets the time the robots.txt file was last fetched to the current
time.
robots.txt
Returns the value of the Crawl-delay parameter from robots.txt for the useragent in question.  If there is no such parameter or it
doesn’t apply to the useragent specified or the robots.txt entry
for this parameter has invalid syntax, return None .
Crawl-delay
robots.txt
robots.txt
Added in version 3.6.
Returns the contents of the Request-rate parameter from robots.txt as a named tuple RequestRate(requests, seconds) .
If there is no such parameter or it doesn’t apply to the useragent specified or the robots.txt entry for this parameter has invalid
syntax, return None .
Request-rate
robots.txt
RequestRate(requests, seconds)
robots.txt
Added in version 3.6.
Returns the contents of the Sitemap parameter from robots.txt in the form of a list() . If there is no such
parameter or the robots.txt entry for this parameter has
invalid syntax, return None .
Sitemap
robots.txt
list()
robots.txt
Added in version 3.8.
The following example demonstrates basic use of the RobotFileParser class:
RobotFileParser

[CODE]
>>> import urllib.robotparser >>> rp = urllib . robotparser . RobotFileParser () >>> rp . set_url ( "http://www.pythontest.net/robots.txt" ) >>> rp . read () >>> rrate = rp . request_rate ( "*" ) >>> rrate . requests 1 >>> rrate . seconds 1 >>> rp . crawl_delay ( "*" ) 6 >>> rp . can_fetch ( "*" , "http://www.pythontest.net/" ) True >>> rp . can_fetch ( "*" , "http://www.pythontest.net/no-robots-here/" ) False
[/CODE]
